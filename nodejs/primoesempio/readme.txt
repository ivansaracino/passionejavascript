Per usare un modulo in un nostro programma, basta usare la funzione globale require('nomemodulo').

Ogni modulo espone delle funzionalità.

Il modulo fs prevede il metodo readFile per leggere il contenuto di un file: il metodo è asincrono, non bloccante.